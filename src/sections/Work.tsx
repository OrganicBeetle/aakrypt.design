import React from 'react';
import WorkPage from '../components/work/WorkPage';

const Work: React.FC = () => {
  return (
    <section id="work">
      <WorkPage />
    </section>
  );
};

export default Work;
